============
Installation
============

At the command line::

    $ easy_install diffservice

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv diffservice
    $ pip install diffservice
